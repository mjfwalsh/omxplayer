#pragma once

enum DBusMethod {
  INVALID_METHOD,
  INVALID_PROPERTY,

  ACTION,
  ASPECT,
  CAN_CONTROL,
  CAN_GO_NEXT,
  CAN_GO_PREVIOUS,
  CAN_PAUSE,
  CAN_PLAY,
  CAN_QUIT,
  CAN_RAISE,
  CAN_SEEK,
  CAN_SET_FULLSCREEN,
  DURATION,
  FULLSCREEN,
  GET_SOURCE,
  GET,
  HAS_TRACK_LIST,
  HIDE_SUBTITLES,
  HIDE_VIDEO,
  IDENTITY,
  LIST_AUDIO,
  LIST_SUBTITLES,
  LIST_VIDEO,
  MAXIMUM_RATE,
  MINIMUM_RATE,
  MUTE,
  NEXT,
  OPEN_URI,
  PAUSE,
  PLAY_PAUSE,
  PLAY,
  PLAYBACK_STATUS,
  POSITION,
  PREVIOUS,
  QUIT,
  RAISE,
  RES_HEIGHT,
  RES_WIDTH,
  SEEK,
  SELECT_AUDIO,
  SELECT_SUBTITLE,
  SET_ALPHA,
  SET_ASPECT_MODE,
  SET_LAYER,
  SET_POSITION,
  SET,
  SHOW_SUBTITLES,
  STOP,
  SUPPORTED_MIME_TYPES,
  SUPPORTED_URI_SCHEMES,
  UN_HIDE_VIDEO,
  UNMUTE,
  VIDEO_STREAM_COUNT,
  VOLUME,

  GET_ROOT_CAN_RAISE,
  GET_ROOT_FULLSCREEN,
  GET_ROOT_HAS_TRACK_LIST,
  GET_PLAYER_MINIMUM_RATE,
  GET_PLAYER_MAXIMUM_RATE,
  GET_PLAYER_RATE,
  GET_PLAYER_VOLUME,
  GET_PLAYER_METADATA,
};

enum DBusMethod dbus_find_method(const char *method_name);

enum DBusMethod dbus_find_property(const char *name);
